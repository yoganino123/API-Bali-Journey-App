const tempImageRoutes = require("express").Router();
const { TempImageController } = require("../../controllers");

// tempImageRoutes.get("/", TempImageController.getTempImage);
// tempImageRoutes.get("/:id", TempImageController.getTempImage);
// tempImageRoutes.post("/", TempImageController.addTempImage);
// tempImageRoutes.delete("/:id", TempImageController.deleteTempImage);
// tempImageRoutes.put("/:id", TempImageController.updateTempImage);

module.exports = tempImageRoutes;
